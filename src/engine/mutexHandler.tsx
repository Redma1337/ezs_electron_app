class MutexHandler {
    public static addMutexToActivity(activityId: number, mutexName: string) {
        // dispatch action to add mutex to activity
    }
    public static connectActivities(sourceId: number, targetId: number) {
        // dispatch action to connect activities
    }
    public static addActivity(task: string) {
        // dispatch action to add activity
    }
}